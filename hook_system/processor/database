To directly access the database on the server:

`sudo -i -u stromae psql -d hookserver`

To dump the contents of the database:

Login in stromae:

```
su stromae
```

then run:

```
pg_dump -U stromae -W -F t hookserver > PATH\TO\FILE\hook-dump.tar
```

